spring:
  application:
    name: yaquedo-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/yaquedo}
    username: ${DB_USER:yaquedo}
    password: ${DB_PASSWORD:yaquedo_local_pwd}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USER:}
    password: ${SMTP_PASS:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${PORT:8080}
  servlet:
    context-path: /

yaquedo:
  jwt:
    secret: ${JWT_SECRET:yaquedo-dev-secret-please-change-in-production-min-32-chars}
    expiration-ms: 86400000
  openstreetmap:
    base-url: https://nominatim.openstreetmap.org
    user-agent: YaQuedo/2.0 (yaquedo@tetradev.pe)
  ai:
    provider: groq
    base-url: https://api.groq.com/openai/v1
    api-key: ${GROQ_API_KEY:}
    model: llama-3.3-70b-versatile

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha

logging:
  level:
    com.tetradev.yaquedo: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
